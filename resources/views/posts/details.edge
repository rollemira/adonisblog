@layout('main')

@section('content')
  <a href="/posts">Go Back</a>
  <hr>
  <h1>{{post.title}}</h1>
  <p>{{post.body}}</p>
  <hr>
  <a href="/posts/edit/{{post.id}}" class="btn btn-lg btn-dark d-block float-left">Edit Post</a>

  <form action="{{route('PostController.destroy', {id: post.id})}}?_method=DELETE" method="POST">
    {{csrfField()}}
    <button class="btn btn-lg btn-danger d-block float-right" type="submit">Delete Post</button>
  </form>
@endsection
